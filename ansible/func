ansible-playbook(){
  del_stopped ansible

  $DOCKER_COMMAND run --rm -it \
    -v ${HOME}/.ssh:/root/.ssh:ro \
    -v ${PWD}:/ansible_playbooks \
    --name ansible \
    $DOCKER_REPO_PREFIX/ansible $*
}
